# Ferramentas

SEFAZ RS: Débitos Veiculares expõe 1 ferramenta (todas somente leitura).

### 1. `sefaz_rs_debitos_consultar`
**Input**: `placa`, `renavam`, `login_cpf` (opcional), `login_senha` (opcional), `pkcs12_cert` (opcional), `pkcs12_pass` (opcional)

SEFAZ RS: Débitos Veiculares, consulta em fonte oficial.

## Prompts de exemplo

```
Consultar SEFAZ RS: Débitos Veiculares
```
